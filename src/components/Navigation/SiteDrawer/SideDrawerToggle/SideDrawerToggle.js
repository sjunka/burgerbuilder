import React from "react";
import classes from "./sideDrawerToggle.css";

const sideDrawerToggle = props => {
  return (
    <div className={classes.DrawerToggle} onClick={props.abrirMenu}>
      <div />

      <div />

      <div />
    </div>
  );
};

export default sideDrawerToggle;
